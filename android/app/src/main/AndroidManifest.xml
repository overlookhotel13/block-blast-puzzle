<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  Required permissions, activities, and AdMob metadata for Block Blast Puzzle.
  TODO: Replace the AdMob App ID below with your real App ID before release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for AdMob and any network-based features -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Optional: prevents the app appearing on devices without touch screens -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="true"/>

    <application
        android:label="Block Blast Puzzle"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Splash screen -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Do not delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!--
          TODO: Replace "ca-app-pub-3940256099942544~3347511713" with your real AdMob App ID
          before release.  Get your App ID from the AdMob console:
          https://admob.google.com

          The value below is Google's official test App ID — safe for development.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

</manifest>
